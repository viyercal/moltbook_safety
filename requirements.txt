# Molt Observatory Requirements
# Core dependencies for scraping, evaluation, and reporting

# HTTP and scraping
playwright>=1.40.0
requests>=2.31.0

# Environment
python-dotenv>=1.0.0

# Data visualization (for reports)
plotly>=5.18.0

# Data processing
pandas>=2.0.0

# Database (optional, for PostgreSQL sync)
psycopg2-binary>=2.9.9

# Airflow (install separately or via constraints)
# apache-airflow>=2.8.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
